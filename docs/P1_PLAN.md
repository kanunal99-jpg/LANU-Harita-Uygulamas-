# P1 Navigation Consolidation

P1 is executed as one consolidated change set. The first target is real on-device offline routing using Valhalla Mobile with validated regional tilesets. No synthetic route is permitted.
